<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
	
<mapper namespace="lhdt.anals.hello.persistence.HelloMapper">

	<!--  1건 조회 -->
	<select id="findById" resultType="lhdt.anals.hello.domain.Hello">
		SELECT	h.hello_id AS helloId
			,h.hello_name AS helloName
			,h.cn
		FROM hello h
		WHERE	hello_id = #{helloId}
		LIMIT 1
	</select>
	
	
	<!-- 전체 목록 조회 -->
	<select id="findAll" resultType="lhdt.anals.hello.domain.Hello">
		SELECT	h.hello_id AS helloId
			,h.hello_name AS helloName
			,h.cn
		FROM hello h
		ORDER BY h.hello_name
	</select>
	
	
	<!--  동적 조회 조건 -->
	<sql id="w">
		<if test="null != searchHelloName and '' != searchHelloName">
			AND	h.hello_name LIKE '%' || #{searchHelloName} || '%' 
		</if>
	</sql>
	
	
	<!-- 전체 건수 -->
	<select id="findTotcnt" resultType="int">
		SELECT COUNT(*)
		FROM	hello h
		WHERE	1=1
		<include refid="w"/>
	</select>
	
	
	<!-- 페이징 -->
	<select id="findByPage" resultType="lhdt.anals.hello.domain.Hello">
		SELECT t.*
		FROM (
			SELECT	h.hello_id AS helloId
				,h.hello_name AS helloName
				,h.cn
			FROM hello h
			WHERE	1=1
			<include refid="w"/>
			ORDER BY h.hello_name) t
		LIMIT #{pageSize}
		OFFSET #{offset}			
	</select>
</mapper>